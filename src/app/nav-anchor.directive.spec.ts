import { NavAnchorDirective } from './nav-anchor.directive';

describe('NavAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new NavAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
